---
import { TokenSearch } from "@/components/TokenSearch";
import SiteFooter from "@/components/SiteFooter.astro";
import faviconUrl from "@/images/favicon.ico";
const faviconHref = typeof faviconUrl === "string" ? faviconUrl : faviconUrl.src;
import "@/styles/global.css";
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Search Magic tokens and token-producing cards." />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" href={faviconHref} />
    <title>Tokens | MTG Card Forge</title>
  </head>
  <body>
    <main>
      <TokenSearch client:load />
    </main>
    <SiteFooter />
  </body>
</html>
